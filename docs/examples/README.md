# Examples

Worked examples will live here. Until then, see the **Common workflows** section in the top-level [README](../../README.md).

Planned:

- `blog/` — a tiny blogging schema (users + posts) projected end-to-end.
- `migration/` — adding a column and round-tripping the result.
- `ci-validation/` — a GitHub Actions snippet that runs `sql-md-sync validate --round-trip` on every PR.
